// Color config
export const CSS_COLOR_PREFIX = '--color'

// Alphas generated for each color in percentage[]
export const alphas = [0, 15, 50, 70, 80]

// Brightens generated for each color in percentage[]
export const ups = [10, 150, 200]

// Darkens generated for each color in percentage[]
export const downs = []
