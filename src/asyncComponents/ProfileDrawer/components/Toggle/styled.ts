'use client'

import styled from 'styled-components'

export const Wrapper = styled.div`
  position: absolute;
  bottom: calc(var(--space) / 2);
  left: calc(100vw - 80px - var(--space) / 2);
  width: var(--space);
  height: var(--space);
  border-radius: 50%;
  cursor: pointer;
`
